const DbService = require("moleculer-db");
const { events: event{{camelCaseName}} } = require("../{{newAggregatePath}}");

module.exports = {
  name: "{{serviceName}}",

  mixins: [DbService],

  adapter: new DbService.MemoryAdapter({ filename: ":memory:" }),

  /**
   *
   * Service settings
   */
  settings: { fields: ["_id", "title"] },

  /**
   * Service dependencies
   */
  dependencies: [],

  /**
   * Actions
   */
  actions: {
    async dispose() {
      return this.adapter.removeMany({});
    },
  },

  /**
   * Events
   */
  events: {
    [event{{camelCaseName}}.types.CREATED](event) {
      this.actions.create({
        _id: event.aggregateId,
        title: event.payload.title,
      });
    },
    [event{{camelCaseName}}.types.DELETED](event) {
      this.actions.remove(event.aggregateId);
    },
  },

  /**
   * Methods
   */
  methods: {},

  /**
   * Service created lifecycle event handler
   */
  created() {},

  /**
   * Service started lifecycle event handler
   */
  started() {},

  /**
   * Service stopped lifecycle event handler
   */
  stopped() {},
};
